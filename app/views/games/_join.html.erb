
<% if game.winner.blank? && participants.count < game.members %>
  <aside>
    <p>
    <% if policy(game).join? %>
        <%= render "games/participants", game: game%>
    <% end %>

    <% if current_user %>
      <h3> Wanna add friend ? </h3>
      <%= render "participants/new", game: game, participant: Participant.new%>
    <% end %>

    <% if current_user %>
      <%= turbo_stream_from "user_#{user.id}_friends" %>
        <%= render "games/game_invites/invite_friends", user: @user, friends: @eligible_friends, game: game %>
    <% end %>
    </p>
  </aside>
<% end %>
