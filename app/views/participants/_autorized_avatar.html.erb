<% if participant&.avatar&.attached? %>
  <%=link_to (image_tag (participant.avatar),
              style: "display: height: 35px;
                                 width:35px;"), "/profiles/#{participant.profile}" %>
<% else %>
  <%= link_to (image_tag "guestavatar1.png",style: "display: height: 35px;
                                                             width:35px;"), "/profiles/#{participant.profile}"%>
<% end %>
